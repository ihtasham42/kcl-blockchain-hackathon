// We generate a membership certificate for our users, so they can go anywhere else on polygon with their certificate to verify that they are a major contributor to a project. 

// We generate a Polygon-ID certificate for our users. 


// Users can generate a polygon-id to verify elsewhere that they are major contributors or hold particular badges. 

// You can quickly verify that a user has a certain amount of references or badges. A quick-check. 
